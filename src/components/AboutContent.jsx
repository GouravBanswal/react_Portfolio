// AboutContent.jsx — Preserved for backward compatibility.
// The About page now uses Experience + Certifications components directly.
import React from 'react'

const AboutContent = () => null

export default AboutContent